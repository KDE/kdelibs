@PACKAGE_INIT@

find_dependency(Qt5Widgets @REQUIRED_QT_VERSION@)
find_dependency(XmlGui "${PACKAGE_VERSION}")
find_dependency(KIOCore "${PACKAGE_VERSION}")
find_dependency(KTextWidgets "${PACKAGE_VERSION}")

set(KParts_INSTALL_PREFIX "@PACKAGE_CMAKE_INSTALL_PREFIX@")
set_and_check(KParts_INCLUDE_DIR    "@PACKAGE_INCLUDE_INSTALL_DIR@")

include("${CMAKE_CURRENT_LIST_DIR}/KPartsTargets.cmake")

set(KParts_LIBRARIES KF5::KParts)
set(KParts_INCLUDE_DIRS ${KParts_INCLUDE_DIR} )
