# Copy library, images and catalogue to ./images directory
# (one per document directory, alternatively one big directory)
# The crucial bit is that the library and the catalogue are
# in one directory which does NOT contain _SGML_ files.
