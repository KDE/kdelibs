
KDE_FIND_PATH(jw, JW, [
   AC_MSG_WARN([You miss jade wrapper. Install docbook-utils from ftp.kde.org/pub/kde/devel/docbook ])
   AC_MSG_WARN([or create a little script named 'jw' that calls jade with the correct catalogues depending on the ])
   AC_MSG_WARN([given DTD. This is a bit tricky, so better install docbook-utils. ])
])

catdir=$kde_datadir/ksgmltools
AC_SUBST(catdir)

dtddir=$catdir/dtd
AC_SUBST(dtddir)

dnl Don't remove !
dnl AC_OUTPUT(ksgmltools/kdb2html)
