@PACKAGE_INIT@

find_dependency(Qt5Core @REQUIRED_QT_VERSION@)
find_dependency(KCoreAddons "${PACKAGE_VERSION}")

set(KPTY_INSTALL_PREFIX "@PACKAGE_CMAKE_INSTALL_PREFIX@")
set_and_check(KPTY_INCLUDE_DIR    "@PACKAGE_INCLUDE_INSTALL_DIR@")

include("${CMAKE_CURRENT_LIST_DIR}/KPtyTargets.cmake")

set(KPTY_LIBRARIES KF5::KPty)
set(KPTY_INCLUDE_DIRS ${KPTY_INCLUDE_DIR} )
