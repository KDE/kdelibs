# These are the rules used by makekdedoc to generate the kdelibs
# documentation with KDOC. Please update them if I forget any
# dependencies.
#
# -Taj (Apr 1999)
#
# $Id$

kde_MODULES = dcop kdefx kdecore kdeui kio kded kssl kfile kparts kjs khtml kdejava kspell kab kdeprint libkmid interfaces

dcop_FILES = *.h
dcop_LIBS = -lqt

kdefx_FILES = *.h
kdefx_LIBS = -lqt

kdecore_FILES = k*.h net*.h
kdecore_LIBS = -lkdefx -ldcop -lqt

kdeui_FILES = *.h
kdeui_LIBS = -lkdecore -lkdefx -ldcop -lqt

kab_FILES = kabapi.h addressbook.h
kab_LIBS = -lkdecore -lkdeui -lqt

kfile_FILES= *.h
kfile_PATH = kio/kfile
kfile_LIBS= -lkio -lkdecore -lkdeui -lkdefx -lqt

kspell_FILES = *.h
kspell_LIBS = -lkdecore -lkdeui -lkdefx -lqt

kjs_FILES = value.h types.h object.h interpreter.h operations.h ustring.h
kjs_LIBS = -lqt

khtml_FILES = *.h dom/*.h
khtml_LIBS = -lkio -lkparts -lkdecore -lkdeui -lkdefx -lqt

kdejava_FILES = *.h
kdejava_PATH = khtml/java
kdejava_LIBS = -lkdecore -lkdeui -lkdefx -lqt

kio_FILES = *.h
kio_PATH = kio/kio
kio_LIBS = -lkdecore -lkdeui -lkdefx -ldcop -lqt

kded_FILES = *.h
kded_PATH = kio/kded
kded_LIBS = -lkio -lkdecore -lkdeui -lkdefx -ldcop -lqt

kssl_FILES = *.h
kssl_PATH = kio/kssl
kssl_LIBS = -lkdeui -lkdefx -ldcop -lqt

kparts_FILES = *.h
kparts_LIBS = -lkdeui -lkdefx -lkdecore -lqt

kdeprint_FILES = kprinter.h kprintdialogpage.h kpreloadobject.h
kdeprint_LIBS = -lkdefx -lkdeui -lkdecore -lqt

libkmid_FILES = *.h
libkmid_LIBS = -lqt

interfaces_FILES = */*.h
interfaces_LIBS = -lkparts -lkdeui -lqt
