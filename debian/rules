#!/usr/bin/make -f
# Made with the aid of debmake, by Christoph Lameter,
# based on the sample debian/rules file for GNU hello by Ian Jackson.

SHELL=/bin/sh

BUILDDIR = ./build

build: $(BUILDDIR)/build-stamp

-include debian/debiandirs

debian/debiandirs: admin/debianrules
	perl -w admin/debianrules echodirs > debian/debiandirs

$(BUILDDIR)/build-stamp:
	dh_testdir
	if test ! -f configure; then \
	  $(MAKE) -f admin/Makefile.common ;\
	fi
	test -d $(BUILDDIR) || mkdir $(BUILDDIR) ;\
	cd $(BUILDDIR) ;\
	$$PWD/../configure $(configkde) \
	--libdir=/$(kde_libdir) --includedir=/$(kde_includedir) 

	make -C $(BUILDDIR)

	touch $(BUILDDIR)/build-stamp

debian-clean:
	dh_testdir
	-rm -f $(BUILDDIR)/build-stamp
	dh_clean

clean: debian-clean
	-rm -rf $(BUILDDIR)
	-rm -f debian/debiandirs debian/undocumented 
	if test -d CVS; then \
	  make -f admin/Makefile.common cvs-clean ;\
	fi

binary-indep:	build
	dh_testdir
# There are no architecture-independent files to be uploaded
# generated by this package.  If there were any they would be
# made here.

binary-arch:  build
	dh_testdir
	dh_testroot
	dh_clean 
	-rm -f debian/undocumented
	dh_installdirs
	install -d debian/tmp/usr/share/applnk
	install -d debian/tmp/etc/pam.d
	make -C $(BUILDDIR) DESTDIR=`pwd`/debian/tmp/ install
	debianrc=debian/tmp/$(kde_confdir)/system.kdeglobals ;\
	install -d `dirname $$debianrc` ;\
	echo "[Directories]" > $$debianrc ;\
	echo "dir_config=/$(kde_confdir)" >> $$debianrc ;\
	echo "dir_html=/$(kde_htmldir)" >> $$debianrc ;\
	echo "dir_apps=/$(kde_appsdir)" >> $$debianrc 
	cp debian/tmp/$(kde_confdir)/system.kdeglobals debian/tmp/etc/kderc
	perl -w admin/debianrules kdelibs3
	cp debian/kde.pamd debian/tmp/etc/pam.d/kde
	for i in `find debian/tmp/$(kde_bindir) | sed -e 's#^debian/tmp/$(kde_bindir)/##'  | grep -v "debian/tmp"`; do \
	  echo "$$i.1" >> debian/undocumented ; \
	done
	for i in `cat debian/undocumented.in | grep -v "#"`; do \
	  echo "$$i.1" >> debian/undocumented ; \
	done
	dh_installdocs
	if test -f /usr/bin/qt2kdoc; then \
	  (mkdir -p debian/tmp/usr/share/kdoc/index) ; \
	  (mkdir -p debian/kdelibs3-doc/usr/share/doc/kdelibs3-doc/html/kdoc-reference) ; \
	  qt2kdoc --url=/usr/share/doc/qt2.2-doc/doc/html \
                  --outdir=`pwd`/debian/kdelibs3-doc/usr/share/doc/kdelibs3-doc/html/kdoc-reference \
                  --compress \
                  /usr/share/doc/qt2.2-doc/doc/html ; \
	  makekdedoc --libdir=`pwd`/debian/kdelibs3-doc/usr/share/doc/kdelibs3-doc/html/kdoc-reference \
                     --kdocopt='--skip-internal --compress' \
                     --outputdir=`pwd`/debian/kdelibs3-doc/usr/share/doc/kdelibs3-doc/html ; \
	fi

# libraries not starting with lib are modules
	dh_movefiles -pkdelibs3-dev $(kde_includedir) $(kde_libdir)/lib*.a $(kde_libdir)/lib*.la
	dh_installchangelogs
	dh_undocumented
	dh_strip
	-rm debian/tmp/usr/share/doc/kde/HTML/en/kspell/common/ 
	cd debian/tmp/usr/share/doc/kde/HTML/en/kspell && ln -s ../common common
	dh_perl
	dh_link
	dh_compress 
	dh_fixperms 
	dh_suidregister 
	dh_makeshlibs
	dh_installdeb
	dh_shlibdeps 
	dh_gencontrol
	cd debian/tmp && find ./$(kde_confdir) -type f -print | sed -e 's#^./#/#' > DEBIAN/conffiles
	echo "/etc/pam.d/kde" >> debian/tmp/DEBIAN/conffiles
	chmod 644 debian/tmp/DEBIAN/conffiles
	dh_md5sums
	perl -w admin/debianrules update
	dh_builddeb

# Below here is fairly generic really

binary:		binary-indep binary-arch

.PHONY: binary binary-arch binary-indep clean
