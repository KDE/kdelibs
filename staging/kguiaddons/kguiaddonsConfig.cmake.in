@PACKAGE_INIT@

# Any changes in this file will be overwritten by CMake.

if(NOT TARGET KF5::kconfigcore)
   set(kconfig_NOT_FOUND_MESSAGE "kguiaddons depends on the package kconfig, which is missing. Use find_package() to find it before kguiaddons.")
   set(kconfig_FOUND FALSE)
   return()
endif()

set(kguiaddons_VERSION_MAJOR "@KGUIADDONS_VERSION_MAJOR@")
set(kguiaddons_VERSION_MINOR "@KGUIADDONS_VERSION_MINOR@")
set(kguiaddons_VERSION_PATCH "@KGUIADDONS_VERSION_PATCH@")

set(kguiaddons_INSTALL_PREFIX "@PACKAGE_CMAKE_INSTALL_PREFIX@")
set_and_check(kguiaddons_INCLUDE_DIR    "@PACKAGE_INCLUDE_INSTALL_DIR@")

if(NOT TARGET KF5::kguiaddons)
  include("${CMAKE_CURRENT_LIST_DIR}/kguiaddonsTargets.cmake")
endif()

set(kguiaddons_LIBRARIES KF5::kguiaddons)
set(kguiaddons_INCLUDE_DIRS ${kguiaddons_INCLUDE_DIR} )
