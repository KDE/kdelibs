@PACKAGE_INIT@

# Any changes in this file will be overwritten by CMake.
find_dependency(Qt5Core @REQUIRED_QT_VERSION@)
find_dependency(Qt5::Widgets @REQUIRED_QT_VERSION@)
find_dependency(Qt5::Xml @REQUIRED_QT_VERSION@)
find_dependency(KCoreAddons "${PACKAGE_VERSION}")
find_dependency(KService "${PACKAGE_VERSION}")

set(KEmoticons_INSTALL_PREFIX "@PACKAGE_CMAKE_INSTALL_PREFIX@")
set_and_check(KEmoticons_INCLUDE_DIR    "@PACKAGE_INCLUDE_INSTALL_DIR@")

# un-comment this once the dependencies have been cleaned:
include("${CMAKE_CURRENT_LIST_DIR}/KEmoticonsTargets.cmake")

set(KEmoticons_LIBRARIES KF5::KEmoticons)
set(KEmoticons_INCLUDE_DIRS ${KEmoticons_INCLUDE_DIR} )
