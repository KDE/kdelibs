@PACKAGE_INIT@

# Any changes in this ".cmake" file will be overwritten by CMake, the source is the ".cmake.in" file.

find_dependency(KJobWidgets "${PACKAGE_VERSION}")
find_dependency(KService "${PACKAGE_VERSION}")
find_dependency(KCompletion "${PACKAGE_VERSION}")
find_dependency(Qt5Network "@QT_REQUIRED_VERSION@")

set_and_check(KIO_INCLUDE_DIR    "@PACKAGE_INCLUDE_INSTALL_DIR@")

include("${CMAKE_CURRENT_LIST_DIR}/KIOTargets.cmake")

set(KIO_CORE_LIBRARY KF5::KIOCore)
set(KIO_LIBRARIES KF5::KIOCore)
set(KIO_INCLUDE_DIRS ${KIO_INCLUDE_DIR})
