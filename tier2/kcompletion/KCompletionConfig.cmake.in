@PACKAGE_INIT@

find_dependency(Qt5Widgets @REQUIRED_QT_VERSION@)
find_dependency(KConfigWidgets "${PACKAGE_VERSION}")
find_dependency(KF5::KI18n "${PACKAGE_VERSION}")

set(KCompletion_INSTALL_PREFIX "@PACKAGE_CMAKE_INSTALL_PREFIX@")
set_and_check(KCompletion_INCLUDE_DIR    "@PACKAGE_INCLUDE_INSTALL_DIR@")

include("${CMAKE_CURRENT_LIST_DIR}/KCompletionTargets.cmake")

set(KCompletion_LIBRARIES KF5::KCompletion)
set(KCompletion_INCLUDE_DIRS ${KCompletion_INCLUDE_DIR} )
